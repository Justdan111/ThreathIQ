import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { SidebarProvider } from "@/components/sidebar-provider";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "ThreatIQ - Community-Driven Security Intelligence",
  description: "ThreatIQ is a security-first platform that empowers users to report and respond to incidents with ease. With role-based access control, secure reporting, and a dark-mode UI designed for safety and clarity, ThreatIQ is trusted by leading security teams worldwide.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
      <SidebarProvider>{children}</SidebarProvider>
      </body>
    </html>
  );
}
